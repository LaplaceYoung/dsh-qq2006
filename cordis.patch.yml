# dsh-qq2006 bundle patch: insert the QQ2006 skin plugin as a new roster
# row. A bare `{ id, name }` entry is an id-targeted overlay and is skipped
# when `ui-skin-qq2006` is not already in the composition (see #5).
- insert:
    - id: ui-skin-qq2006
      name: '@dsh-external/dsh-qq2006'
