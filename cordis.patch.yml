# dsh-qq2006 bundle patch: mount the QQ2006 skin plugin as a client-plugin
# row in the browser roster. Applied after dsh-base and dsh-web-app; the
# row joins the client plugin list beside ui-theme (which owns the theme
# registry this skin registers into).

- id: ui-skin-qq2006
  name: '@dsh-external/dsh-qq2006'
